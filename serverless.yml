---
service: sls-test-svc

provider:
  name: aws
  runtime: nodejs8.10

functions:
  currentTime:
    handler: handler.HTTPendpoint
    events:
      - http:
          path: ping
          method: get
